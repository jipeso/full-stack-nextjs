import { type Config } from "prettier";

const config: Config = {
  semi: false,
  singleQuote: true,
  trailingComma: "es5",
  arrowParens: "avoid",
  jsxSingleQuote: true,
};

export default config;
